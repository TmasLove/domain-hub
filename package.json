{
  "name": "domain-project",
  "version": "0.0.1",
  "description": "Domain Flipping & Management Suite — monorepo root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"node backend/server.js\""
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
